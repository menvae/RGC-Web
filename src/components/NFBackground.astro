
<style is:inline>
    canvas {
            width: 100%;
            height: 100vh;
            position: fixed;
            user-select: none;
            pointer-events: none;
            z-index: -1;
            mix-blend-mode: multiply;
            opacity: 0.2;
        }

        .gradient-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background: linear-gradient(to bottom, #1a0520 0%, transparent 10%, transparent 90%, #1a0520 100%);
            pointer-events: none;
            z-index: -1;
        }
</style>

<canvas id="NF_bg"></canvas>
<div class="gradient-overlay"></div>

<script src="./404_bg.ts"></script>
